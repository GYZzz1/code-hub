server:
  port: 5000

spring:
  cloud:
    gateway:
      routes:
        - id: oss
          uri: lb://jc-club-oss
          predicates:
            - Path=/oss/**
          filters:
            - StripPrefix=1